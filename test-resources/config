;
; Mbwatch configuration
;

notify-command = notify - --audio="/home/guns/.sounds/new-message.mp3"
sync-timer-period = 5m
connection-period = 15m
connection-timeout = 2s
imap-socket-timeout = 5s

; vim:ft=dosini:
